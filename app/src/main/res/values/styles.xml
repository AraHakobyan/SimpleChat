<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TextStyle" parent="Widget.AppCompat.TextView"/>

    <style name="TextStyle.Title">
        <item name="android:textSize">@dimen/text_size_special</item>
        <item name="android:textColor">@color/title_text_color</item>
        <item name="android:padding">@dimen/padding_normal</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="RoundedButton" parent="Widget.AppCompat.TextView">
        <item name="android:background">@drawable/rounded_background</item>
        <item name="android:padding">@dimen/padding_normal</item>
        <item name="android:textColor">@color/white_text_color</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_marginStart">@dimen/margin_big</item>
        <item name="android:layout_marginEnd">@dimen/margin_big</item>
        <item name="android:textSize">@dimen/text_size_special</item>
    </style>

    <style name="TextStyle.ChatItem">
        <item name="android:textSize">@dimen/text_size_chat</item>
        <item name="android:textColor">@color/text_color_black</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">@dimen/padding_normal</item>
    </style>

    <style name="TextStyle.ChatItem.ByMe">
    </style>

    <style name="TextStyle.ChatItem.ByOther">
        <item name="android:layout_marginEnd">@dimen/chat_item_margin</item>
    </style>

    <style name="EditTextStyle">
        <item name="android:background">@drawable/bottom_line</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingBottom">@dimen/padding_normal</item>
        <item name="android:paddingTop">@dimen/padding_normal</item>
        <item name="android:paddingEnd">@dimen/padding_normal</item>
        <item name="android:textSize">@dimen/text_size_chat</item>
        <item name="android:textColorHint">@color/text_color_hint</item>
        <item name="android:textColor">@color/text_color_black</item>
    </style>
</resources>
